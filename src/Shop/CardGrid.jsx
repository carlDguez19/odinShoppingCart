export function CardGrid({products}){
    return(
        <div className="cardGrid">
            {products.map((prod) => {
                //create itemCards here
            })}
        </div>
    )
}